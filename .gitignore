# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Firebase local config/secrets — never commit real project credentials
.env
.env.local
.env.*.local
.firebase/
firebase-debug.log
firestore-debug.log
database-debug.log
ui-debug.log
*-debug.log

# Service-account keys for scripts/exportStatsToSheets.ts (FIREBASE_SERVICE_ACCOUNT_PATH) —
# never commit these; local Application Default Credentials (gcloud auth
# application-default login) is the normal path and needs no key file at all.
serviceAccountKey*.json
*service-account*.json
*-firebase-adminsdk-*.json

# Playwright test artifacts (screenshots/traces from local e2e runs)
test-results/
playwright-report/
blob-report/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
