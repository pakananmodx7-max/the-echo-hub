import type { WhoAmIEntry } from '../types'

// Thai songs from the 1990s.
//
// Left empty deliberately: exact song-title attributions from this era are outside
// what could be verified with confidence during this build. Per the "accuracy over
// quantity" requirement, guessed titles were cut rather than included. See the
// implementation report for details — this category needs a maintainer with access
// to a verified Thai music database to populate safely.
export const THAI_90S_MUSIC: WhoAmIEntry[] = []
